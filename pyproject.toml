[project]
name = "table-color-simulator"
version = "0.1.0"
description = "A local Pillow web app for simulating table-area color combinations."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio>=6.27.0",
    "numpy>=1.26",
    "pillow>=10.0",
]

[tool.uv]
package = false
